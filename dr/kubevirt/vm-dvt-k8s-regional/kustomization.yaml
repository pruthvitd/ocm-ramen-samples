---
resources:
  - ../base
namespace: vm-dvt
commonLabels:
  app: vm-dvt
patches:
  - target:
      kind: DRPlacementControl
      name: drpc
    patch: |-
      - op: replace
        path: /spec/preferredCluster
        value: dr1
