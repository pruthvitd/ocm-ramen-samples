---
# Cluster-scoped resources
# These resources are retained by ACM during disaster recovery operations
# They should be deployed once per cluster and not moved with the application
resources:
  - custom-scc.yaml
  - sc.yaml
commonLabels:
  appname: sftpgo
  scope: cluster

# Made with Bob
