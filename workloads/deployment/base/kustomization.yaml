---
# Main kustomization that references both cluster-scoped and namespace-scoped resources
# For disaster recovery operations:
# - cluster-scoped resources are retained by ACM on the cluster
# - namespace-scoped resources move with the application
resources:
  - cluster-scoped
  - namespace-scoped

# Made with Bob
